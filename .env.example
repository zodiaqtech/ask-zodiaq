# ── Ask ZodiaQ Environment Variables ──────────────────────────────
# Path to the AI-predigest project (the source of astrological evaluators)
# This is the DEPLOYED path where the main API server runs from
AI_PREDIGEST_PATH=/Users/sankit/Desktop/Personalised Horoscope

# Server — use a port different from the main AI-predigest server (8001)
HOST=0.0.0.0
PORT=8002
